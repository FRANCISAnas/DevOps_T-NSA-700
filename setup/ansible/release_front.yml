---
- hosts: front
  tasks:
    - name: stop nginx server
      service: name=nginx state=stopped enabled=no
    - name: deleting destination folder
      file:
        state: absent
        path: "/var/www/front"
    - name: recreating destination folder
      file:
        state: directory
        path: "/var/www/front"
    - name: Copy front build directory
      copy: src=/home/gitlab-runner/builds/XJGkXp2X/0/root/t-nsa/frontend/dist/front dest=/var/www
    - name: Move files one level higher
      shell: mv /var/www/front/* /var/www
    - name: restart nginx server
      service: name=nginx state=restarted enabled=yes
